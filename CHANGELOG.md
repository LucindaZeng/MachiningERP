# Changelog

## [Unreleased]

### Added

- **pnpm monorepo 根**：`pnpm-workspace.yaml`、共享 `tsconfig.base.json`、ESLint 扁平配置与 Prettier；`apps/web` 并入 workspace。
- **`packages/shared`**：前后端共享契约。金额用整数最小货币单位（bigint）+ 币种，数量用 decimal 字符串；含错误码字典、分页与响应包裹、认证契约、权限点字典。
- **`services/api`**：NestJS 模块化单体落地。
  - 平台共享能力：统一单据编号（20 类单据 + 用户唯一编码）、状态机基类、审计日志、节点计时、领域事件出箱、统一通知。
  - `auth` 模块：登录签发 JWT、图形验证码（连续失败 3 次强制、8 次锁定 30 分钟）、注销即撤销会话。
  - `identity` 模块：唯一编码注册即发放且**永不复用**、用户名离职释放可被重新登记、账户申请、密码重置申请（派单 IT，不发邮件/短信）。
  - `org` 模块：十三部门主数据。
  - `quotation` 模块：成本分析计算引擎（公式链对齐 `example/成本分析/CNC成本分析.xls`，含「管理费按小计+损耗计」
    与「中间不取整」两处关键口径）、报价硬校验（强制关联成本分析、强制图纸、低于成本逐档拦截）、
    核价角色闸门（仅报价工程师）、原材料价格表与当日汇率的按日期取价。
  - `quotation` 费率可调：损耗率与管理费利润率的默认 5%/5% 只是初值，报价工程师可在建单、
    改费率（`PUT /cost-analyses/:id/rates`）与重核三条路径上自行调整（如 7% 损耗 + 10% 管理费）；
    锁版后不可改，改动按「5% → 7%」的可读形式入审计。
  - `quotation` 报价单闭环：建单 / 草稿整单替换 / 送审 / 审核 / 驳回 / 成交丢单登记，状态机声明式约束非法迁移；
    **单件成本一律由后端从成本分析推导**（`resolveUnitCosts`，接口不接受调用方自带成本），
    据此在提交时逐档拦截低于成本价；**模具费单列不摊入单件价**；审核通过即锁定成本分析版本，
    改价只能派生新版本。成本与毛利按权限**整组缺席**式下发，业务员的响应里没有 `cost` 这一组。
  - `quotation` 报价单修改申请：业务提交目标价与原因 → 报价工程师要么重核（派生 version+1、rootId 指向初版的
    新成本分析），要么驳回（**理由必填**并原样回到提交人工作台）；两者都是终态，重复处理由乐观锁 + 状态机挡下。
  - `masterdata` 模块：客户档案。系统生成编号、国内必填税号、送货地址 ≤5 且恰一默认、付款条件三选一；
    香港 70% 价格按独立权限点做**字段整组缺席**式裁剪；银行账号/付款条件等敏感字段改动转为变更申请，
    审批通过才落库且不得自批自审；`assertReadyForOrder` 提供下单前的档案完整性闸门。
  - Prisma schema 覆盖平台底座与 auth/identity/org；seed 载入部门、权限点、角色矩阵、编号规则与演示账号。
- **模块化铁律的自动化保障**：ESLint 尺寸红线与分层依赖约束 + `tools/check-module-boundaries.mjs`（controller 路由数、模块目录约定、DTO 单一导出）+ AppModule 依赖注入编译测试。
- 440 个单元测试；核心算法目录分支覆盖率 ≥ 90%（`modules/quotation/services` 分支 95.8%）（登录风控、编号发号、唯一编码、账户申请、客户校验与权限裁剪、成本分析计算链均达标）。

### Changed

- `apps/web` 认证契约与客户契约改为从 `@machining-erp/shared` 再导出；权限点由登录响应下发，未登录时才回退演示角色。
- `apps/web` 客户页面切到真实契约：`hk` 分组缺席即代表无权限，前端不再自行推导；付款条件与发票种类改走枚举中文映射。
- `apps/web` 按尺寸红线拆分：`sales.types.ts` 按 11 个模块拆到 `types/sales/`；订单/BOM/ECN 四个页面抽出子组件；登录、账户申请、下单三个 composable 拆出纯函数与子 composable。

### Fixed

- `apps/web` 演示账号补齐 `permissions`，与后端角色-权限矩阵口径一致。

## [Unreleased]

- ERP V1.9 强制工序级成本核算：切料、CNC/车床、后工序、委外、检验、组装、全检、包装及返工重做分别建立成本卡。
- 工序转入成本、本工序新增成本、累计/单位成本、标准实际差异、在制/报废分配、委外暂估与差异重算规则。
- 工序成本完整性预警、月结阻断、成本瀑布、在制成本、差异与AI下钻分析。
- ERP V1.8 香港公司代生产特殊订单：终端客户100%成交价格、内地工厂70%结算、香港公司30%留存、实际制造成本和工厂毛利。
- 内地工厂系统生成报关资料，交易主体与金额来源按批准版本控制；管理分成、报关、开票、收汇和法定收入分层勾稽。
- 香港公司结算对账、分成差额、报关单证一致性、关联资料、专项预警、门户和AI分析需求。
- ERP V1.7 返工子订单拆分、全部重复工序重新计费、责任成本归集、供应商追加应付与无偿返工控制。
- CNC责任返工的二次表处正常形成应付；委外供应商责任返工追加应付为零，但仍核算供应商造成的企业毛损失、追偿和净损失。
- ERP V1.6 模具订单、样品订单和正式业务订单三分类，以及独立的收费方式、费用承担、审批和成本归集规则。
- 模具/样品允许收费、免费、部分收费、返还或正式订单抵扣/分摊；正式业务订单必须收费并强制形成应收义务。
- 正式订单零价/缺付款条件阻断、免费订单超批准成本预警，以及模具成本回收、样品转化和正式订单回款报表。
- ERP V1.5 从新客户报价、工程准备、业务订单、双重审核、订单评审、MRP、采购、收料、生产、逐工序品质确认、数量交接、委外表处到全检包装的订单全生命周期。
- 订单T0/T1、节点基线/最新/实际时间、首次响应、排队、处理、暂停、总历时、P90/P95及超期升级规则。
- CNC申报100、打磨实收94时先转94、差异6留在CNC预警的数量交接控制，以及品质只判质量不判数量的职责分离。

## [0.3.0] - 2026-07-11

### Added

- ERP V1.4 十部门具体功能、审核与授权权限、预警信息和系统生成报表详细清单。
- 全公司职责分离、分级授权、变更重审、审批超期升级和阻断级预警规则。
- 可复用的部门控制数据源与 Markdown 控制矩阵生成工具。

## [0.2.0] - 2026-07-11

### Added

- ERP V1.3 十部门组织与应用蓝图。
- CNC/车床、后工序、独立组装的工作中心与执行需求。
- 首检、巡检、末件检、FAI电子质量档案和系统报告。
- 行政考勤采集、异常分析、考勤报告和多劳务结算。

## [0.1.0] - 2026-07-10

### Added

- ERP V1.2 规划基线与文档生成工具。
- 业务模块、跨工序返工、详细财务、AI 问答和实时金属材料价格需求。
- 产品、架构、流程、路线图和数据安全文档结构。
- GitHub Issue/PR 模板、仓库检查工作流和依赖更新配置。

## V2.3

- 业务订单由三分类扩展为四分类：新增备料订单——不含交货义务、完工入库即完成、正式订单关联消耗、平均生产成本按（备料单件成本×消耗数＋本单单件成本×新产数）÷总数加权结转。
- 备料订单无论金额大小必须经总经办批准（业务提出→业务经理确认→财务审核资金占用→总经办批准）。
- 业务部新增按客户生成对账单功能：出货、发票、收款、退货折让与未收余额自动汇总，客户回签，数据与财务应收一致。

## V2.2

- 部门蓝图由10个部门扩展为13个：业务部、仓库和IT由职能角色升级为独立部门，并按功能、审核权限、预警和报表四个维度补齐完整清单。
- 新增「模块开发顺序」章节：平台先行（IT）→ 主数据 → 业务部 → 工程部 → PMC执行环（供给侧/执行侧/品质返工闭环）→ 行政 → 财务 → 总经办 → 门户/AI。
- 同步更新仓库 Markdown 文档、部门控制矩阵生成工具和前端文案至十三部门口径。

## V2.1

- 查漏补缺：新增尾数返工与入库（订单交付后剩余数量四条处理路径与结案数量平衡校验）。
- 新增长时间工单未完结监控：账龄分层、停滞预警、疑似死单强制完结评审与月结前应关未关清单。
- 新增架机表与机台负荷管理：自动生成初版架机表、PMC 调整留痕、共用资源冲突、架机率与平衡化生产。
- 明确中途改工序控制与工程变更联动；AI 问答明确选型 DeepSeek V4 Pro（DeepSeek API/TUI）。
- 新增 ADR-0003 技术栈、开发文档（模块划分与单一职责规范）、接口规范与接口文档、TODO 与项目开发顺序进度表。

## V2.0

- 将香港代生产需求收窄为客户主数据价格规则：只有已勾选客户的正式业务订单按输入价格70%自动计价。
- 明确样品订单、模具订单及未勾选客户均保持原输入价格，且BOM、审核、MRP、采购、生产、品质、报关、开票和收款流程全部不变。
- 删除独立70%/30%分成、香港留存、特殊公司间结算和专用门户流程，增加原价/计算价版本、适用性校验、异常预警和验收案例。

本项目采用 [Semantic Versioning](https://semver.org/) 思路记录规划与软件版本；在首个可运行版本前，版本号可能快速调整。
