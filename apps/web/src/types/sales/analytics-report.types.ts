/**
 * 类型已迁到 `@machining-erp/shared`（development-guide §1）。
 * 这里只做再导出，`@/types/sales.types` 桶与各面板组件的引用路径因此不变。
 */
export type {
  ArAgingRow,
  BacklogRow,
  CostVarianceRow,
  CustomerActivityRow,
  CustomerMarginRow,
  CustomerRankRow,
  FunnelRow,
  InvoiceReceivableRow,
  LateReason,
  LostReason,
  MixRow,
  NewCustomerRow,
  OnTimeRow,
  OrderMixRow,
  OrderTrendRow,
  PriceTrendRow,
  ProductMarginRow,
  QuoteByDim,
  QuoteCycleRow,
  RepeatIssueRow,
  RmaStatRow,
  SalesReports,
  ShipmentAchieveRow,
} from '@machining-erp/shared'
