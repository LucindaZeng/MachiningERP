# 贡献指南

## 1. 先确认变更属于什么

- 产品需求：新增或调整业务能力、角色、规则、报表、预警、权限或验收。
- 缺陷：实现结果不符合已批准需求、数据口径或验收案例。
- 数据/集成：主数据、外部接口、迁移、行情、设备、银行、税务或门户集成。
- 架构决策：影响系统边界、部署、数据库、事件、权限、可用性或技术栈。
- 文档：规划方案、流程、字典、ADR、实施或测试资料。

请使用 `.github/ISSUE_TEMPLATE` 中相应表单，避免仅提交一句话需求。

## 2. 需求与分支

每项功能应关联需求编号，例如 `MES-13`、`FIN-22`、`MKT-01`、`AIX-04`。新增需求先在 Issue 中提出，再进入 Word/Markdown 基线。

建议分支命名：

- `feature/MKT-01-metal-price-feed`
- `fix/MES-13-rework-routing`
- `docs/FIN-ai-acceptance`
- `chore/repository-checks`

## 3. 完成定义

功能变更至少说明并验证：

- 角色和组织数据范围；
- 正常流程、异常流程和撤销/冲销方式；
- 关键字段、状态、版本和审计日志；
- 与 ERP、PLM、MES、QMS、WMS、SRM、财务或外部系统的接口；
- 待办、超期、升级和关闭；
- 系统生成文件、指标和钻取链路；
- 权限、敏感数据和职责分离；
- 单元、集成、端到端、权限和回归测试；
- 数据迁移、兼容性和上线回退影响。

## 4. Pull Request

PR 应保持单一目的，填写模板并关联 Issue。涉及以下内容时必须请对应业务负责人复核：

- 报价、采购、库存、成本或财务数字；
- 工艺路线、工序防跳、返工或质量放行；
- 客户、供应商、工资、银行、成本和价格权限；
- 外部行情、银行、税务、设备或 AI 数据接口；
- 指标定义、AI 提示、语义层或管理报表。

提交信息建议使用 Conventional Commits，例如 `feat(mkt): add price snapshot model`。

## 5. 数据与秘密

禁止提交真实客户图纸、供应商底价、合同、个人信息、工资、银行账户、访问令牌、生产数据库备份或未脱敏日志。示例数据必须虚构或不可逆脱敏。

## 6. 文档生成

修改 `tools/build_erp_plan.py` 后应重新生成 Word、更新版本与目录页码，并使用文档渲染工具检查全部页面。QA 中间文件应保留在 `.tmp/`，不提交到 Git。
