# 安全政策

MachiningERP 涉及客户图纸、供应商价格、生产工艺、质量、库存、财务、银行和个人数据，应按企业核心业务系统保护。

## 报告安全问题

请不要通过公开 Issue 披露漏洞、凭证或真实业务数据。优先使用 GitHub 的私有安全报告功能；若仓库尚未启用，请私下联系仓库所有者并只提供最小必要信息。

报告应包含受影响模块、版本/提交、复现条件、潜在影响和建议缓解措施。不要在未经授权的生产环境执行破坏性验证。

## 必须保护的数据

- 客户图纸、合同、报价和项目进度；
- 供应商身份、底价、报价、银行账户和付款信息；
- BOM、工艺、程序、材料批次、设备与质量记录；
- 成本、毛利、工资、预算、凭证、税务和银行数据；
- 金属行情数据的许可凭据、API 密钥和受限原始数据；
- AI 对话、引用单据、提示模板、模型凭据和审计日志；
- 个人信息、访问令牌、密钥、证书、备份和生产日志。

## 安全开发基线

- 最小权限、组织/行/字段级隔离和职责分离；
- 外部用户、管理员和 AI 查询均在数据执行层校验权限；
- 敏感操作二次确认、审批、不可抵赖审计和异常预警；
- 秘密存放在受管秘密系统，不写入代码、配置示例或 Actions 日志；
- 接口采用认证、授权、加密、幂等、限流、重试和可审计错误队列；
- 备份加密并定期验证恢复时间和恢复点；
- AI 默认只读，不得绕过原审批、记账、付款、放行或数据隔离。

## 支持范围

安全修复优先应用于当前受支持的主分支和正式发布版本。规划文档与原型不应被误认为已达到生产安全标准。
